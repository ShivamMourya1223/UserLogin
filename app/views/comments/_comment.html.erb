<% post = Post.find_by(id:post)%>
<% post.comments.each do |comment|%>
    
	<p><%= comment.comment %>
	   <%#= comment.likes.count%><br>

	   <%# pre_like = comment.likes.find { |like| like.user_id == current_user.id} %>
	  <%# if pre_like %>
		<%#= link_to "LikeComment", post_comment_likes_path(@post,comment_id: comment.id), method: :post ,remote:true %>
	   <%#end%>
	</p>

<% end %>